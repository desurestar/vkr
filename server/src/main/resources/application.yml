server:
  port: 8080
  servlet:
    session:
      cookie:
        http-only: true
        secure: false
      timeout: 7d

spring:
  session:
    store-type: jdbc
    timeout: 7d
    jdbc:
      initialize-schema: never
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 25MB
  datasource:
    url: jdbc:postgresql://localhost:5432/vkr
    username: klows
    password: 190485
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  media:
    upload-dir: media
